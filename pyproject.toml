[tool.ruff]
target-version = "py310"
line-length = 110
exclude = [".venv*", "server/lib", "server/include"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.ruff.format]
quote-style = "single"
